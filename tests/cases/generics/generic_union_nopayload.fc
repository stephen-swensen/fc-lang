union maybe =
    | just('a)
    | nothing

let main = (args: str[]) ->
    let x = maybe<int32>.just(42)
    let y = maybe<int32>.nothing
    match x with
    | just(v) ->
        match y with
        | just(_) -> 0
        | nothing -> v
    | nothing -> 0
