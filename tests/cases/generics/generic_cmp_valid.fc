let greater = (x: 'a, y: 'a) -> x > y

let main = (args: str[]) ->
    let _result =
        let mut r = 0
        if greater(10, 5) then r = r + 1 else r = r
        if greater(3.14, 2.71) then r = r + 2 else r = r
        r
    assert(_result == 3)
    0
