// An enum *variant* is a value of the enum type, not an integer, so only the
// enum's `count` folds into a const argument.
enum dir of u8 =
    | north
    | south

struct buf =
    data: u8['n]

let main = (args: str[]) ->
    let a = default(buf<dir.north>)
    assert(a.data.len == 1)
    0
