// Passing a generic function directly as an argument is likewise rejected.
let identity = (x: 'a) -> x
let apply_it = (f: (i32) -> i32, n: i32) -> f(n)

let main = (args: str[]) ->
    apply_it(identity, 5)
