import containers from acme::

let main = (args: str[]) ->
    let o = containers.create(10, 20)
    assert(o.ptr->x == 10)
    assert(o.ptr->y == 20)
    assert(containers.sum_inner(o) == 30)
    free(o.ptr)
    0
