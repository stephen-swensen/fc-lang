union result =
    | good('a)
    | bad(i32)

let make_good = (x: 'a) -> result<'a>.good(x)

let main = (args: str[]) ->
    let _result =
        let r = make_good(42)
        match r with
        | good(v) -> v
        | bad(e) -> e
    assert(_result == 42)
    0
