// A float type property is a compile-time constant but not an integer one, so
// it cannot stand as a const generic argument.
struct buf =
    data: u8['n]

let main = (args: str[]) ->
    let a = default(buf<f64.nan>)
    assert(a.data.len == 1)
    0
