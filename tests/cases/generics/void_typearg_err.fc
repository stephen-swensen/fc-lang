// void has no value representation, so binding a type parameter to it would
// monomorphize a copy taking `void x`. The inferred spelling — passing a
// void-returning call to a generic function — is rejected at the argument.
let idv = (x: 'a) -> x

let v = () -> return

let main = (args: str[]) ->
    idv(v())
    0
