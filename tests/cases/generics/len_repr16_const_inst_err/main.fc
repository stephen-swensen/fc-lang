// A const-generic instantiation whose folded fixed-array size exceeds the
// stored width is a per-instance error, reported through the
// instantiation-chain diagnostics.
struct wide =
    limbs: u32['n / 32]
let main = (args: str[]) ->
    let w = default(wide<2000000>)
    (i32) w.limbs.len
