// const type in expression-level generic type arg list (exercises is_type_arg_token)

let make = <'a>() -> default('a)

let main = (args: str[]) ->
    let x = make<const i32*>()
    assert(x == (const i32*) 0isize)
    0
