struct config =
    name: const str
    value: int32

let get_name_len = (c: config) ->
    c.name.len

let main = (args: str[]) ->
    let c = config { name = "test", value = 42 }
    let len = get_name_len(c)
    c.value + (int32) len - 42 - 4
