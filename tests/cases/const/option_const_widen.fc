// Option inner widening: i32*? widens to const i32*?

let read_opt = (p: const i32*?) ->
    match p with
    | some(ptr) -> *ptr
    | none -> 0

let main = (args: str[]) ->
    let mut x = 77
    let p = some(&x)        // i32*? (non-const)
    read_opt(p) - 77        // widens to const i32*?
