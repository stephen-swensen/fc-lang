let use_const = (p: const int32*) ->
    *p

let use_const_slice = (s: const int32[]) ->
    s[0]

let main = (args: str[]) ->
    let mut x = 42
    let p = &x
    let v1 = use_const(p)
    let arr = int32[2] { 7, 8 }
    let v2 = use_const_slice(arr)
    v1 + v2 - 42 - 7
