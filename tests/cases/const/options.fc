let deref_opt = (p: const int32*?) ->
    match p with
    | some(ptr) -> *ptr
    | none -> 0

let main = (args: str[]) ->
    let mut x = 55
    let p = some((const int32*)&x)
    deref_opt(p) - 55
