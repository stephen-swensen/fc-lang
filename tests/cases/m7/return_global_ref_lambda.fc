let scale = 2

let make_scaler = (args: int32) ->
    (x: int32) -> x * scale

let apply = (f: (int32) -> int32, x: int32) -> f(x)

let main = (args: int32) ->
    let f = make_scaler(0)
    apply(f, 21)
