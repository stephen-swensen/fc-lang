namespace vendor::

module shapes =
    struct rect =
        w: i32
        h: i32

    module rect =
        let area = (r: shapes.rect) -> r.w * r.h
        let square = (side: i32) -> shapes.rect { w = side, h = side }
