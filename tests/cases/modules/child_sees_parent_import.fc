module helpers =
    let double = (x: int32) -> x * 2

module parent =
    import double from helpers

    module child =
        let quad = (x: int32) -> double(double(x))

let main = (args: str[]) ->
    assert(parent.child.quad(5) == 20)
    0
