module mod_a =
    let value = 10

module mod_b =
    let value = 20

module parent =
    import value from mod_a

    module child =
        import value from mod_b
        let get = () -> value

let main = (args: str[]) ->
    assert(parent.child.get() == 20)
    0
