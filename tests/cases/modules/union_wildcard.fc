module shapes =
    union shape =
        | circle(int32)
        | square(int32)
    let area = (s: shape) -> match s with
        | circle(r) -> r
        | square(s) -> s

import * from shapes

let main = (args: str[]) ->
    let c = shape.circle(5)
    assert(area(c) == 5)
    0
