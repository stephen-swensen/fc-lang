import shapes from geometry
import area_sq from shapes

module geometry =
    module shapes =
        let area_sq = (side: int32) -> side * side

let main = (args: str[]) ->
    assert(area_sq(7) == 49)
    0
