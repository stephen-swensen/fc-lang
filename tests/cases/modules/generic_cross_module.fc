module containers =
    let identity = (x: 'a) -> x

module geo =
    struct point =
        x: i32
        y: i32

let main = (args: str[]) ->
    let p = geo.point { x = 20, y = 22 }
    let q = containers.identity(p)
    assert(q.x + q.y == 42)
    0
