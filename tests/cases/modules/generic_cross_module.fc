import identity from containers

module containers =
    let identity = (x: 'a) -> x

module geo =
    struct point =
        x: int32
        y: int32

let main = (args: str[]) ->
    let p = geo.point { x = 20, y = 22 }
    let q = identity(p)
    assert(q.x + q.y == 42)
    0
