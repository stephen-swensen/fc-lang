import identity from utils
import pair_first from utils

module utils =
    let identity = (x: 'a) -> x
    let pair_first = (a: 'a, b: 'b) -> a

let main = (args: str[]) ->
    let a = identity(42)
    let b = pair_first(10, true)
    assert(a + b == 52)
    0
