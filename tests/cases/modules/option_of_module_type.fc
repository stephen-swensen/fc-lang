module m =
    struct item =
        value: i32

let find = (x: i32) ->
    if x > 0 then some(m.item { value = x })
    else none(m.item)

let main = (args: str[]) ->
    let _result =
        match find(42) with
        | some(it) -> it.value
        | none -> 0
    assert(_result == 42)
    0
