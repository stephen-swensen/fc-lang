module math =
    let add = (a: int32, b: int32) -> a + b

module util =
    let double = (x: int32) -> math.add(x, x)

let main = (args: str[]) ->
    let x = math.add(3, 4)
    assert(util.double(x) == 14)
    0
