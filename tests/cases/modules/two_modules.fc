module math =
    let add = (a: i32, b: i32) -> a + b

module util =
    let double = (x: i32) -> math.add(x, x)

let main = (args: str[]) ->
    let x = math.add(3, 4)
    assert(util.double(x) == 14)
    0
