import * from outer

module inner =
    let secret = 42

module outer =
    import secret from inner
    let get_secret = () -> secret
    let public_val = 10

let main = (args: str[]) ->
    assert(public_val + get_secret() == 52)
    0
