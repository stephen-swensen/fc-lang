module inner =
    let secret = 42

module outer =
    import secret from inner
    let get_secret = () -> secret
    let public_val = 10

import * from outer

let main = (args: str[]) ->
    public_val + get_secret()
