module geo =
    struct point =
        x: i32
        y: i32

    let origin = () -> point { x = 0, y = 0 }
    let offset = (p: point, dx: i32, dy: i32) -> point { x = p.x + dx, y = p.y + dy }

let main = (args: str[]) ->
    let p = geo.offset(geo.origin(), 20, 22)
    assert(p.x + p.y == 42)
    0
