namespace lib::

module core =
    let apply = (x: 'a, f: ('a) -> 'a) -> f(x)
    let identity = (x: 'a) -> x

module app =
    module engine =
        import apply from core
        import identity from core
        let inc_twice = (x: i32) ->
            apply(apply(x, (n: i32) -> n + 1), (n: i32) -> n + 1)
        let pass = (x: i32) -> identity(x)
