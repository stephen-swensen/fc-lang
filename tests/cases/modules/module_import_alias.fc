module math =
    let add = (a: i32, b: i32) -> a + b

module calculator =
    import add as plus from math
    let compute = (a: i32, b: i32) -> plus(a, b)

let main = (args: str[]) ->
    assert(calculator.compute(13, 29) == 42)
    0
