module math =
    let add = (a: int32, b: int32) -> a + b

module calculator =
    import add as plus from math
    let compute = (a: int32, b: int32) -> plus(a, b)

let main = (args: str[]) ->
    assert(calculator.compute(13, 29) == 42)
    0
