namespace mylib::

module helpers =
    let value = 42
    let inc = (x: int32) -> x + 1

module outer =
    // Nested module accesses namespace sibling directly (no import needed)
    module inner =
        let get = () -> helpers.value
        let apply = () -> helpers.inc(helpers.value)
