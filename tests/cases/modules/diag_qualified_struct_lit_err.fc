// Struct literal field error on module-scoped struct: should show "m.point"
module m =
    struct point =
        x: i32
        y: i32

let main = (args: str[]) ->
    let p = m.point { x = 1, z = 2 }
    0
