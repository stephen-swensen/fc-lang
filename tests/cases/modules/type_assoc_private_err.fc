module geometry =
    struct vec2 =
        x: int32
        y: int32

    module vec2 =
        private let internal = (v: vec2) -> v.x

import vec2 from geometry

let main = (args: str[]) ->
    let p = vec2 { x = 3, y = 4 }
    vec2.internal(p)
