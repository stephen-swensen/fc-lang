import * from utils

module utils =
    let identity = (x: 'a) -> x
    let add_one = (x: int32) -> x + 1

let main = (args: str[]) ->
    let _result =
        let a = identity(10)
        let b = identity(true)
        let c = add_one(a)
        if b then c else 0
    assert(_result == 11)
    0
