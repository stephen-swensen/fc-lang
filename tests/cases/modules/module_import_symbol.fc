// Module-scoped import: import a specific symbol from a module
module math =
    let add = (a: int32, b: int32) -> a + b
    let sub = (a: int32, b: int32) -> a - b

module wrapper =
    import add from math
    let compute = () -> add(30, 12)

let main = (args: str[]) ->
    wrapper.compute()
