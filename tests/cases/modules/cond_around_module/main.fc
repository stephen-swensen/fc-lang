#if with_extra
module extra =
    let bonus = 2
#end

module base =
    let value = 40

let main = (args: str[]) ->
    let mut _r = 0
#if with_extra
    _r = base.value + extra.bonus
#else
    _r = base.value
#end
    assert(_r == 42)
    0
