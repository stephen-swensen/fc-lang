namespace acme::

module rng =

    struct pcg =
        state: u64

    module pcg =
        let make = (seed: u64) ->
            pcg { state = seed }

        let next = (r: pcg*) ->
            r->state = r->state * 6364136223846793005u64 + 1u64
            (u32) (r->state >> 32)
