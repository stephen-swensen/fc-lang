module utils =
    let identity = (x: 'a) -> x
    let add_one = (x: i32) -> x + 1

module logic =
    import identity from utils
    import add_one from utils
    let process = (x: i32) -> identity(add_one(x))

let main = (args: str[]) ->
    assert(logic.process(9) == 10)
    0
