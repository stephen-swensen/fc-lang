module math =
    module ops =
        let add = (a: int32, b: int32) -> a + b
        let mul = (a: int32, b: int32) -> a * b

import ops from math
import * from ops

let main = (args: str[]) ->
    add(mul(5, 8), 2)
