module geometry =
    struct point =
        x: int32
        y: int32

import point from geometry

let make = (x: int32, y: int32) -> point { x = x, y = y }

let main = (args: str[]) ->
    let p = make(10, 20)
    assert(p.x + p.y == 30)
    0
