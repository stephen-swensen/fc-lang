module lib =
    union wrapper =
        | val('a)
        | empty

let check = (a: lib.wrapper<i32>, b: lib.wrapper<i32>) ->
    if a == b then 10
    else 20

let main = (args: str[]) ->
    let a = lib.wrapper<i32>.val(42)
    let b = lib.wrapper<i32>.val(42)
    assert(check(a, b) == 10)
    0
