// import * from middle should NOT bring 'secret' — it's middle's import, not its own decl
import * from middle

module inner =
    let secret = 42

module middle =
    import secret from inner
    let get = () -> secret

let main = (args: str[]) ->
    assert(get() == 42)
    0
