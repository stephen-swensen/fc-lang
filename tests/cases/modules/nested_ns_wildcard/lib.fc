namespace vendor::

module utils =
    let add = (a: i32, b: i32) -> a + b
    let mul = (a: i32, b: i32) -> a * b

module app =
    module core =
        import * from utils
        let compute = (x: i32, y: i32) -> add(x, mul(y, 2))
