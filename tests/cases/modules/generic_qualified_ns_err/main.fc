import types from lib::

module local =
    struct pair =
        first: 'a
        second: 'b

let main = (args: str[]) ->
    let a = types.pair { first = 1, second = 2i8 }
    let b = local.pair { first = 1, second = 2i8 }
    if a == b then 1 else 0
