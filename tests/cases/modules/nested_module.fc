module geometry =
    module shapes =
        let area = (w: i32, h: i32) -> w * h

    let scale = (x: i32) -> x * 2

let main = (args: str[]) ->
    let a = geometry.shapes.area(3, 4)
    let s = geometry.scale(5)
    assert(a + s == 22)
    0
