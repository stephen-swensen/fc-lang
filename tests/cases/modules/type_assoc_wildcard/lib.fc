module geometry =
    struct vec2 =
        x: i32
        y: i32

    module vec2 =
        let zero = vec2 { x = 0, y = 0 }
        let add = (a: vec2, b: vec2) -> vec2 { x = a.x + b.x, y = a.y + b.y }
