module math =
    let add = (a: i32, b: i32) -> a + b
    let mul = (a: i32, b: i32) -> a * b

module calculator =
    import * from math
    let compute = (a: i32, b: i32) -> add(a, mul(a, b))

let main = (args: str[]) ->
    assert(calculator.compute(3, 4) == 15)
    0
