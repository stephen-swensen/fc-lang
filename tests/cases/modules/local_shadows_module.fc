// Local let binding shadows a module with the same name.
module data =
    let value = 99

module m =
    let test = () ->
        let data = 42
        data + 1

let main = (args: str[]) ->
    assert(m.test() == 43)
    assert(data.value == 99)
    0
