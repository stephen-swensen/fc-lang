// Three-level nesting: bottom accesses ancestors' private members
module top =
    private let base = 10

    module mid =
        private let multiplier = 3

        module bottom =
            let compute = () -> base * multiplier

let main = (args: str[]) ->
    assert(top.mid.bottom.compute() == 30)
    0
