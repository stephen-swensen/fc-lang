import point from geometry

module geometry =
    struct point =
        x: int32
        y: int32

    let manhattan = (p: point) -> p.x + p.y

let main = (args: str[]) ->
    let p = point { x = 10, y = 20 }
    assert(geometry.manhattan(p) == 30)
    0
