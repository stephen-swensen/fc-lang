import * from geometry

module geometry =
    struct vec2 =
        x: int32
        y: int32

    let make = (x: int32, y: int32) -> vec2 { x = x, y = y }

let main = (args: str[]) ->
    let v = make(10, 20)
    assert(v.x + v.y == 30)
    0
