module a =
    import z from c
    let x = (n: i32) -> z(n)

module b =
    import x from a
    let y = (n: i32) -> x(n)

module c =
    import y from b
    let z = (n: i32) -> y(n)

let main = (args: str[]) ->
    a.x(5)
