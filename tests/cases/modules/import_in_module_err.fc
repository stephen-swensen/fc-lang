module helper =
    let double = (x: i32) -> x * 2

module broken =
    import double from helper

    let use_it = (x: i32) -> double(x)

let main = (args: str[]) ->
    assert(broken.use_it(5) == 10)
    0
