namespace acme::

// File-level imports are visible to all modules in this file
import value from helpers
import double from helpers

module helpers =
    let value = 42
    let double = (x: i32) -> x * 2

module consumer =
    let get = () -> value
    let apply = (x: i32) -> double(x)
