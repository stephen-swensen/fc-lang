// Private bindings are visible to sibling members within the same module
module math =
    private let helper = (x: int32) -> x * 10
    let compute = (x: int32) -> helper(x) + 5

let main = (args: str[]) ->
    assert(math.compute(4) == 45)
    0
