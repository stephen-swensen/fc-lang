// Module-scoped symbol import should NOT be visible outside the module
module math =
    let add = (a: int32, b: int32) -> a + b

module wrapper =
    import add from math
    let compute = () -> add(1, 2)

let main = (args: str[]) ->
    add(10, 20)
