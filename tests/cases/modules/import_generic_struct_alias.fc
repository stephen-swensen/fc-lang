module containers =
    struct box =
        value: 'a

    let wrap = (x: 'a) -> box { value = x }
    let unwrap = (b: box<'a>) -> b.value

import box as wrapper from containers

let main = (args: str[]) ->
    let b = containers.wrap(42)
    assert(containers.unwrap(b) == 42)
    0
