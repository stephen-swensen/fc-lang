module shapes =
    union shape =
        | circle(i32)
        | rect(i32)

    module shape_ops =
        let area_approx = (s: shape) ->
            match s with
            | circle(r) -> r * r * 3
            | rect(side) -> side * side
