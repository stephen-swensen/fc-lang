module math =
    let add = (a: i32, b: i32) -> a + b
    let mul = (a: i32, b: i32) -> a * b

let main = (args: str[]) ->
    let x = math.add(3, 4)
    let y = math.mul(x, 2)
    assert(y == 14)
    0
