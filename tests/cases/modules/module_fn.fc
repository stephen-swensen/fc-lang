module math =
    let add = (a: int32, b: int32) -> a + b
    let mul = (a: int32, b: int32) -> a * b

let main = (args: str[]) ->
    let x = math.add(3, 4)
    let y = math.mul(x, 2)
    assert(y == 14)
    0
