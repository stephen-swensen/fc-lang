# Skipped in the -O2 test runs (test-*-O2). This test asserts the FULL frame
# set (inner + outer + main); at -O2 `outer`, which tail-calls `inner`, is
# elided by the C compiler's TCO. Full frames are an -O0/low-opt fidelity
# property. The -O2 behavior is covered by backtraces/unwrap_chain_o2.
