// Realistic pattern: alloc + defer free inside a loop
let main = (args: str[]) ->
    let mut total = 0
    for i in 0..3
        let p = alloc(int32[1] { i })!
        defer free(p)
        total = total + p[0]
    assert(total == 3)  // 0 + 1 + 2
    0
