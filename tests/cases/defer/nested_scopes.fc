// Verify nested defer scopes: inner defers run before outer
let mut trace = 0

let main = (args: str[]) ->
    defer trace = trace * 10 + 1
    for i in 0..1 do
        defer trace = trace * 10 + 2
    // After for: trace = 0*10+2 = 2
    // After main: trace = 2*10+1 = 21
    0
