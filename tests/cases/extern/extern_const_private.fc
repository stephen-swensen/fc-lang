// Test: private extern constants are not accessible outside the module
module c from "fcntl.h" =
    private extern O_RDONLY: i64
    extern O_WRONLY: i64

let main = (args: str[]) ->
    // Public constant is accessible
    let wr = c.O_WRONLY
    assert(wr >= 0)
    0
