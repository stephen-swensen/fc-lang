// Test: conflicting define values for same macro is a compile error
module t1 from "time.h" define "_POSIX_C_SOURCE" "200809L" =
    extern clock_gettime: (int32, any*) -> int32

module t2 from "time.h" define "_POSIX_C_SOURCE" "199309L" =
    extern nanosleep: (any*, any*?) -> int32

let main = (args: str[]) ->
    0
