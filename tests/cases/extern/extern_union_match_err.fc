// Test: pattern matching on extern union is an error
module u from "stdint.h" =
    extern union dummy =
        x: int32
        y: int32

import dummy from u

let main = (args: str[]) ->
    let d = default(dummy)
    match d with
    | { x = v } -> v
