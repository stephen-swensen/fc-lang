// Test: pattern matching on extern union is an error
module u from "stdint.h" =
    extern union dummy =
        x: int32
        y: int32

let main = (args: str[]) ->
    let d = default(u.dummy)
    match d with
    | { x = v } -> v
