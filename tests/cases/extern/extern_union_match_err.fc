// Test: pattern matching on extern union is an error
import dummy from u

module u from "stdint.h" =
    extern union dummy =
        x: int32
        y: int32

let main = (args: str[]) ->
    let d = default(dummy)
    match d with
    | { x = v } -> v
