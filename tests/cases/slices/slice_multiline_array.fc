// Multiline slice literal
let main = (args: str[]) ->
    let _result =
        let arr = i32[3]{
            10,
            20,
            30
        }
        arr[0] + arr[1] + arr[2]
    assert(_result == 60)
    0
