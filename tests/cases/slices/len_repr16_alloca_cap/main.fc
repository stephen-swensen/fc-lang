// A runtime-sized alloca slice over capacity traps before the stack buffer
// is dimensioned.
let main = (args: str[]) ->
    let mut n = 40000i64
    n = n + 0
    let s = alloca(u8[n] { })
    (i32) s.len
