// Test: slice literal with more elements than declared length is rejected
// (would otherwise write past the alloca backing buffer)
let main = (args: str[]) ->
    let buf = i32[3] { 1, 2, 3, 4 }
    (i32) buf.len
