let main = (args: str[]) ->
    let mut data = int32[4] { 10, 20, 30, 40 }
    let s = int32[] { ptr = data.ptr, len = 4i64 }
    assert(s.len == 4i64)
    assert(s[0] == 10)
    assert(s[1] == 20)
    assert(s[2] == 30)
    assert(s[3] == 40)
    0
