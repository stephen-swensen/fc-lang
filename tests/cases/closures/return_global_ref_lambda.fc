let scale = 2

let make_scaler = (n: int32) ->
    (x: int32) -> x * scale

let apply = (f: (int32) -> int32, x: int32) -> f(x)

let main = (args: str[]) ->
    let f = make_scaler(0)
    assert(apply(f, 21) == 42)
    0
