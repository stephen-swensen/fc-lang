let scale = 2

let make_scaler = (n: i32) ->
    (x: i32) -> x * scale

let apply = (f: (i32) -> i32, x: i32) -> f(x)

let main = (args: str[]) ->
    let f = make_scaler(0)
    assert(apply(f, 21) == 42)
    0
