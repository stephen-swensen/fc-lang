// Non-capturing nested function calls itself by name (self-recursion).
let main = (args: str[]) ->
    let fac = (n: i32) ->
        if n <= 1 then 1
        else n * fac(n - 1)
    assert(fac(0) == 1)
    assert(fac(1) == 1)
    assert(fac(5) == 120)
    assert(fac(10) == 3628800)
    0
