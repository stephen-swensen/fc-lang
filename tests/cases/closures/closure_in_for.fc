let apply = (f: (i32) -> i32, x: i32) -> f(x)

let main = (args: str[]) ->
    let _result =
        let mut sum = 0
        for i in 0..5 do
            let captured = i
            let f = (x: i32) -> x + captured
            sum = sum + apply(f, 10)
        sum
    assert(_result == 60)
    0
