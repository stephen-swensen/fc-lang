import io from std::

let main = (args: str[]) ->
    let _result =
        let f = io.open("nonexistent_xyz_path/fc_open_fail_file.txt", "r")
        match f with
        | some(v) ->
            io.close(v)
            1
        | none -> 42
    assert(_result == 42)
    0
