import io from std::

let main = (args: str[]) ->
    let _result =
        let f = io.open("/tmp/fc_test_io_open_close.txt", "w")
        match f with
        | some(v) ->
            io.write("test\n", v)
            io.close(v)
            42
        | none -> 1
    assert(_result == 42)
    0
