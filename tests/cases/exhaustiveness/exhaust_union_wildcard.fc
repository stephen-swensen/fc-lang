union shape =
    | circle(int32)
    | square(int32)
    | dot

let main = (args: str[]) ->
    let _result =
        let s = shape.circle(5)
        let r = match s with
        | circle(r) -> r
        | _ -> 0
        r
    assert(_result == 5)
    0
