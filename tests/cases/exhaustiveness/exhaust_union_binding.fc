union shape =
    | circle(int32)
    | square(int32)

let main = (args: str[]) ->
    let _result =
        let s = shape.circle(5)
        match s with
        | circle(r) -> r
        | x -> 0
    assert(_result == 5)
    0
