struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let _result =
        let p = point { x = 10, y = 20 }
        match p with
        | { x = a, y = b } -> a + b
    assert(_result == 30)
    0
