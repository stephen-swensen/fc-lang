union result =
    | good('a)
    | bad('b)

let check = (r: result<i32, i32>) ->
    match r with
    | good(v) -> v
    | bad(e) -> e

let main = (args: str[]) ->
    assert(check(result<i32, i32>.good(42)) == 42)
    0
