union result =
    | ok('a)
    | err('b)

let check = (r: result<i32, i32>) ->
    match r with
    | ok(v) -> v
    | err(e) -> e

let main = (args: str[]) ->
    assert(check(result<i32, i32>.ok(42)) == 42)
    0
