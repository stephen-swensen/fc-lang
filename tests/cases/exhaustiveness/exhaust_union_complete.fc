union shape =
    | circle(i32)
    | square(i32)
    | dot

let main = (args: str[]) ->
    let _result =
        let s = shape.circle(5)
        let r = match s with
        | circle(r) -> r
        | square(s) -> s
        | dot -> 0
        r
    assert(_result == 5)
    0
