let main = (args: str[]) ->
    let name = "hello world"
    let s = "%.5s{name}!"
    let mut r = 0
    // Should truncate to "hello" (5 chars) + "!"
    if s == "hello!" then r = r + 1 else r = r
    if s.len == 6 then r = r + 2 else r = r
    r
