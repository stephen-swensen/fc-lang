let main = (args: str[]) ->
    let _result =
        let x = 3.14
        let y = 1.0f32
        let s = "%T{x},%T{y}"
        let mut r = 0
        if s == "f64,f32" then r = r + 1
        r
    assert(_result == 1)
    0
