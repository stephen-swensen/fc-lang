let main = (args: str[]) ->
    let _result =
        let x = 42u64
        let s = "val=%u{x}"
        let mut r = 0
        if s == "val=42" then r = r + 1
        let s2 = "%x{x}"
        if s2 == "2a" then r = r + 2
        r
    assert(_result == 3)
    0
