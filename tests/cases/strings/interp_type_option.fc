let maybe = (x: int32) ->
    if x > 0 then some(x)
    else default(int32?)

let main = (args: str[]) ->
    let x = maybe(10)
    let s = "%T{x}"
    let mut r = 0
    if s == "int32?" then r = r + 1 else r = r
    r
