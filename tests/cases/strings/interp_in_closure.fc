// interpolation inside a capturing closure body; promote to heap to return
let main = (args: str[]) ->
    let x = 5
    let y = 7
    let fmt = () -> alloc("val=%d{x * y}")!
    let r = fmt()
    let mut passed = 0
    if r == "val=35" then passed = 1
    free(r)
    assert(passed == 1)
    0
