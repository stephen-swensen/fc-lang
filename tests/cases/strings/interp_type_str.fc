let main = (args: str[]) ->
    let _result =
        let name = "hello"
        let s = "%T{name}"
        let mut r = 0
        if s == "const str" then r = r + 1 else r = r
        r
    assert(_result == 1)
    0
