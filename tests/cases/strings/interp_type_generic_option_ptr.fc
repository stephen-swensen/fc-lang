struct box =
    val: 'a

let main = (args: str[]) ->
    let _result =
        let mut b = box { val = 42 }
        let p = &b
        let s = "%T{p},%T{b}"
        let mut r = 0
        if s == "box<int32>*,box<int32>" then r = r + 1 else r = r
        r
    assert(_result == 1)
    0
