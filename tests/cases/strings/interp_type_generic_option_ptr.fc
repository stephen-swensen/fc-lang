struct box =
    val: 'a

let main = (args: str[]) ->
    let _result =
        let mut b = box { val = 42 }
        let p = &b
        let s = "%T{p},%T{b}"
        let mut r = 0
        if s == "box<i32>*,box<i32>" then r = r + 1
        r
    assert(_result == 1)
    0
