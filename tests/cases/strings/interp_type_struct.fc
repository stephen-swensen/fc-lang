struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let _result =
        let p = point { x = 1, y = 2 }
        let s = "%T{p}"
        let mut r = 0
        if s == "point" then r = r + 1
        r
    assert(_result == 1)
    0
