let main = (args: str[]) ->
    let name = "world"
    let s = "hello %s{name}!"
    let mut r = 0
    if s == "hello world!" then r = r + 1 else r = r
    if s.len == 12 then r = r + 2 else r = r
    r
