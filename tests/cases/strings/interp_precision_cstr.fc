let main = (args: str[]) ->
    let _result =
        let name = (cstr) "hello world"
        let s = "%.5s{name}!"
        let mut r = 0
        if s == "hello!" then r = r + 1
        if s.len == 6 then r = r + 2
        r
    assert(_result == 3)
    0
