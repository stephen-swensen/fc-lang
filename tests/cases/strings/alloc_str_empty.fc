let main = (args: str[]) ->
    let _result =
        let s = alloc("")
        let mut r = 0
        match s with
        | some(v) ->
            if v == "" then r = r + 1
            if v.len == 0 then r = r + 2
        | none -> assert(false)
        r
    assert(_result == 3)
    0
