let main = (args: str[]) ->
    let _result =
        let mut x = 42
        let p = &x
        let s = "%T{p}"
        let mut r = 0
        if s == "int32*" then r = r + 1
        r
    assert(_result == 1)
    0
