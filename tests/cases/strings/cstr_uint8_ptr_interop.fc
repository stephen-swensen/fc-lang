module c from "stdlib.h" =
    extern atoi: (const cstr) -> i32

let parse_num = (p: u8*) ->
    c.atoi(p)

let main = (args: str[]) ->
    let s = "99"
    let cs = alloca(c"%s{s}")
    assert(parse_num(cs) == 99)
    0
