module c from "stdlib.h" =
    extern atoi: (const cstr) -> int32

let parse_num = (p: uint8*) ->
    c.atoi(p)

let main = (args: str[]) ->
    let s = "99"
    let cs = (cstr) s
    assert(parse_num(cs) == 99)
    0
