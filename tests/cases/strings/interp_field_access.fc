// interpolation of struct field-access expressions
struct pt =
    x: i32
    y: i32

let main = (args: str[]) ->
    let p = pt { x = 3, y = 4 }
    let s = "p=(%d{p.x}, %d{p.y})"
    let mut passed = 0
    if s == "p=(3, 4)" then passed = 1
    assert(passed == 1)
    0
