let main = (args: str[]) ->
    let _result =
        let x = 1000000000000i64
        let s = "%d{x}"
        let mut r = 0
        if s == "1000000000000" then r = r + 1 else r = r
        if s.len == 13 then r = r + 2 else r = r
        r
    assert(_result == 3)
    0
