let main = (args: str[]) ->
    let _result =
        let mut buf = u8[5] { }
        buf.ptr[0] = 10u8
        buf.ptr[1] = 20u8
        buf.ptr[2] = 30u8
        buf.ptr[3] = 40u8
        buf.ptr[4] = 50u8

        let len = buf.len
        let v = buf[2]
        let sub = buf[1..3]
        let sub_len = sub.len
        let sub_v = sub[0]
        let p = buf.ptr
        let pv = *p

        let mut total = 0u8
        for x in buf do
            total = total + x

        let result = (i32) len + (i32) v + (i32) sub_len + (i32) sub_v + (i32) pv + (i32) total
        result
    assert(_result == 217)
    0
