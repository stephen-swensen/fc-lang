let main = (args: str[]) ->
    let _result =
        let name = "hello world"
        let s = "%-8.5s{name}!"
        let mut r = 0
        // Left-aligned: "hello   !"
        if s == "hello   !" then r = r + 1
        if s.len == 9 then r = r + 2
        r
    assert(_result == 3)
    0
