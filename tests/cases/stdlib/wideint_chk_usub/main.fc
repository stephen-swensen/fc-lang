import wideint from std::

let main = (args: str[]) ->
    // unsigned subtract underflow (a < b)
    let _r = wideint.u256.checked_sub(wideint.u256.from_u64(1u64), wideint.u256.from_u64(2u64))
    0
