import wideint from std::

let main = (args: str[]) ->
    // unsigned subtract underflow (a < b)
    let _r = wideint.uwide.checked_sub(wideint.uwide<256>.from_u64(1u64), wideint.uwide<256>.from_u64(2u64))
    0
