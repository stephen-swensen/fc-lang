import math from std::

let main = (args: str[]) ->
    // is_nan
    assert(math.is_nan(f64.nan) == true)
    assert(math.is_nan(0.0) == false)
    assert(math.is_nan(f64.inf) == false)
    assert(math.is_nan(1.5) == false)

    // is_inf
    assert(math.is_inf(f64.inf) == true)
    assert(math.is_inf(f64.neg_inf) == true)
    assert(math.is_inf(0.0) == false)
    assert(math.is_inf(f64.nan) == false)
    assert(math.is_inf(1.5) == false)

    // is_finite
    assert(math.is_finite(1.5) == true)
    assert(math.is_finite(0.0) == true)
    assert(math.is_finite(-999.0) == true)
    assert(math.is_finite(f64.inf) == false)
    assert(math.is_finite(f64.neg_inf) == false)
    assert(math.is_finite(f64.nan) == false)

    0
