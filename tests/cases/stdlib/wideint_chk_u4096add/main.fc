import wideint from std::

let main = (args: str[]) ->
    // unsigned add overflow: max + 1
    let _r = wideint.uwide.checked_add(wideint.uwide<4096>.max(), wideint.uwide<4096>.from_u64(1u64))
    0
