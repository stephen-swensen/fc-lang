import wideint from std::

let main = (args: str[]) ->
    // signed subtract overflow (min - 1)
    let _r = wideint.iwide.checked_sub(wideint.iwide<256>.min(), wideint.iwide<256>.from_i64(1i64))
    0
