import wideint from std::

let main = (args: str[]) ->
    // unsigned subtract underflow: 0 - 1
    let _r = wideint.uwide.checked_sub(wideint.uwide<2048>.min(), wideint.uwide<2048>.from_u64(1u64))
    0
