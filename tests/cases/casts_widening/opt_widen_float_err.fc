// f32? must not widen to f64? — same struct-to-struct issue as ints.
let take = (x: f64?) ->
    match x with
    | some(v) -> v
    | none -> 0.0

let main = (args: str[]) ->
    let a = some(1.5f32)
    let r = take(a)
    (i32) r
