// u8? must not widen to u32? (unsigned numeric option widening).
let take = (x: u32?) ->
    match x with
    | some(v) -> v
    | none -> 0u32

let main = (args: str[]) ->
    let a = some(7u8)
    let r = take(a)
    (i32) r
