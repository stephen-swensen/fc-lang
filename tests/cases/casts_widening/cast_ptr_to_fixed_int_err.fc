// A pointer cannot be cast to a fixed-width int (u64 != pointer width on
// every target). Use (usize)/(isize), then chain to narrow.
let main = (args: str[]) ->
    let mut x = 5i32
    let p = &x
    let bad = (u64) p
    assert(bad > 0u64)
    0
