union result =
    | ok('a)
    | err('b)

let check = (r: result<int32, int32>) ->
    match r with
    | ok(v) -> v
    | err(e) -> e

let main = (args: str[]) ->
    check(result<int32, int32>.ok(42))
