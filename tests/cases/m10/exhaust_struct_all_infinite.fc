// Struct where all fields are infinite types — needs wildcard
struct point =
    x: int32
    y: int32

let check = (p: point) ->
    match p with
    | { x = 0, y = 0 } -> 1
    | { x = x, y = y } -> x + y

let main = (args: str[]) ->
    check(point { x = 3, y = 4 })
