let main = (args: str[]) ->
    let _result =
        let a = some(42)
        let b = some(42)
        let c = default(int32?)
        let mut r = 0
        if a == b then r = r + 1
        if !(a != b) then r = r + 2
        if a != c then r = r + 4
        if !(a == c) then r = r + 8
        r
    assert(_result == 15)
    0
