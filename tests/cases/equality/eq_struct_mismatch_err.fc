struct point =
    x: int32
    y: int32

struct pair =
    a: int32
    b: int32

let main = (args: str[]) ->
    let p = point { x = 1, y = 2 }
    let q = pair { a = 1, b = 2 }
    if p == q then 1 else 0
