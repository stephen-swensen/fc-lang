let main = (args: str[]) ->
    let mut x = 10
    let a = some(&x)
    let b = some(&x)
    let c = none(int32*)
    let mut r = 0
    if a == b then r = r + 1 else r = r
    if a != c then r = r + 2 else r = r
    r
