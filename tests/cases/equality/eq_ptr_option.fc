let main = (args: str[]) ->
    let _result =
        let mut x = 10
        let a = some(&x)
        let b = some(&x)
        let c = default(int32*?)
        let mut r = 0
        if a == b then r = r + 1
        if a != c then r = r + 2
        r
    assert(_result == 3)
    0
