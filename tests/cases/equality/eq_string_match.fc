let classify = (s: const str) ->
    match s with
    | "yes" -> 1
    | "no" -> 2
    | _ -> 0

let main = (args: str[]) ->
    assert(classify("yes") + classify("no") + classify("maybe") == 3)
    0
