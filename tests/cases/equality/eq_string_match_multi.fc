let score = (s: const str) ->
    match s with
    | "a" -> 1
    | "bb" -> 2
    | "ccc" -> 3
    | "dddd" -> 4
    | _ -> 0

let main = (args: str[]) ->
    assert(score("a") + score("bb") + score("ccc") + score("dddd") + score("unknown") == 10)
    0
