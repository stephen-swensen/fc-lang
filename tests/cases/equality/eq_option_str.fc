let main = (args: str[]) ->
    let _result =
        let a = some("hello")
        let b = some("hello")
        let c = some("world")
        let d = default(str?)
        let mut r = 0
        if a == b then r = r + 1
        if a != c then r = r + 2
        if a != d then r = r + 4
        r
    assert(_result == 7)
    0
