let main = (args: str[]) ->
    let _result =
        let a = some(42)
        let b = some(42)
        let c = some(99)
        let d = default(int32?)
        let e = default(int32?)
        let mut r = 0
        if a == b then r = r + 1
        if a != c then r = r + 2
        if a != d then r = r + 4
        if d == e then r = r + 8
        r
    assert(_result == 15)
    0
