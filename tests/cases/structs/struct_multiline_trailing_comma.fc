// Trailing comma in multiline struct literal
struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let _result =
        let p = point {
            x = 7,
            y = 8,
        }
        p.x + p.y
    assert(_result == 15)
    0
