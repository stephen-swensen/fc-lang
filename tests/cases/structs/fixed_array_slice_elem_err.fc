// A fixed array may only be a field's outermost type: a *slice of* fixed
// arrays would emit `uint8_t[2]* ptr` — not a C declarator.
struct grid =
    m: u8[2][]

let main = (args: str[]) ->
    assert(sizeof(grid) > 0)
    0
