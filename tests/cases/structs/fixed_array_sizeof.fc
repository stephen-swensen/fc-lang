struct small =
    data: i32[4]

let main = (args: str[]) ->
    // sizeof standalone type
    assert(sizeof(i32[4]) == 16i64)
    assert(sizeof(u8[16]) == 16i64)

    // sizeof struct includes inline array
    assert(sizeof(small) >= 16i64)
    0
