// Multiline struct literal inside a match arm
struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let _result =
        let n = 1
        let p = match n with
            | 1 -> point {
                x = 10,
                y = 20
            }
            | _ -> point {
                x = 0,
                y = 0
            }
        p.x + p.y
    assert(_result == 30)
    0
