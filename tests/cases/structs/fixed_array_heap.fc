struct packet =
    data: uint8[8]
    id: int32

let main = (args: str[]) ->
    let src = uint8[8] { 1u8, 2u8, 3u8, 4u8, 5u8, 6u8, 7u8, 8u8 }
    let p = alloc(packet { data = src, id = 42 })!

    assert(p->data[0] == 1u8)
    assert(p->data[7] == 8u8)
    assert(p->id == 42)

    free(p)
    0
