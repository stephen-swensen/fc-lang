struct point =
    x: i32
    y: i32

struct circle =
    center: point
    radius: i32

let main = (args: str[]) ->
    let c = circle { center = point { x = 10, y = 20 }, radius = 5 }
    let { center = { x = cx, y = cy }, radius = r } = c
    assert(cx + cy + r == 35)
    0
