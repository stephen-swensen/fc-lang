struct buffer =
    data: int32[4]

let read_first = (p: const buffer*) -> p->data[0]

let main = (args: str[]) ->
    let mut b = buffer { data = int32[4] { 42, 0, 0, 0 } }
    assert(read_first(&b) == 42)
    0
