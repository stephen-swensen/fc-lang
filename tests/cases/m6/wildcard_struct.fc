module geometry =
    struct vec2 =
        x: int32
        y: int32

    let make = (x: int32, y: int32) -> vec2 { x = x, y = y }

import * from geometry

let main = (args: int32) ->
    let v = make(10, 20)
    v.x + v.y
