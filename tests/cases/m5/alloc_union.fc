union shape =
    | circle(int32)
    | square(int32)

let main = (args: int32) ->
    let s = alloc(shape)!
    *s = shape.circle(7)
    let r = match *s with
        | circle(v) -> v
        | square(v) -> v
    free(s)
    r
