let classify = (c: char) ->
    match c with
    | 'A' -> 1
    | 'B' -> 2
    | '\n' -> 3
    | '\0' -> 4
    | _ -> 0

let main = (args: str[]) ->
    classify('A') + classify('B') + classify('\n') + classify('\0') + classify('Z')
