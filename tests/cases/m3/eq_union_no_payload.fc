union color =
    | red
    | green
    | blue

let main = (args: int32) ->
    let a = color.red
    let b = color.red
    let c = color.blue
    let mut r = 0
    if a == b then r = r + 1 else r = r
    if a != c then r = r + 2 else r = r
    r
