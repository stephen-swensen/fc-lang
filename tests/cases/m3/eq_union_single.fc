union wrapper =
    | val(int32)

let main = (args: str[]) ->
    let a = wrapper.val(42)
    let b = wrapper.val(42)
    let c = wrapper.val(99)
    let mut r = 0
    if a == b then r = r + 1 else r = r
    if a != c then r = r + 2 else r = r
    r
