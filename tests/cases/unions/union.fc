union shape =
    | circle(i32)
    | square(i32)

let area = (s: shape) ->
    match s with
    | circle(r) -> r * r
    | square(side) -> side * side

let main = (args: str[]) ->
    let s = shape.circle(3)
    let a = area(s)
    assert(a - 2 == 7)
    0
