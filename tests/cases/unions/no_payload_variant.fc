union color =
    | red
    | green
    | blue

let main = (args: str[]) ->
    let _result =
        let c = default(color)
        match c with
        | red -> 10
        | green -> 20
        | blue -> 30
    assert(_result == 10)
    0
