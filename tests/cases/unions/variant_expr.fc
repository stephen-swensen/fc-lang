union color =
    | red
    | green
    | blue

let main = (args: str[]) ->
    let _result =
        let c = color.blue
        match c with
        | red -> 10
        | green -> 20
        | blue -> 42
    assert(_result == 42)
    0
