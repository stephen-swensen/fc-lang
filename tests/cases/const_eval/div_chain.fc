// Chained division evaluates left-to-right.
module m =
    let a = 100 / 5 / 2
    let b = (100 / 5) / 2
    let c = 7 + 40 / 8 * 2

let main = (args: str[]) ->
    assert(m.a == 10)
    assert(m.b == 10)
    assert(m.c == 17)
    0
