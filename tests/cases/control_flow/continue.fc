// Sum of odd numbers 1..10 using continue to skip evens
let main = (args: str[]) ->
    let _result =
        let mut sum = 0
        for i in 0..10
            if i % 2 == 0 then continue
            sum = sum + i
        sum
    assert(_result == 25)
    0
