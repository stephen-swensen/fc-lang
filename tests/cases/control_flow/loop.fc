let main = (args: str[]) ->
    let _result =
        let mut sum = 0
        let mut i = 0
        loop
            if i >= 10 then break
            sum = sum + i
            i = i + 1
        sum
    assert(_result == 45)
    0
