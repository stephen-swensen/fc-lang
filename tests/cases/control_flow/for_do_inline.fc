// Inline `for` body is introduced by `do`.
let main = (args: str[]) ->
    let mut sum = 0
    for i in 0..5 do sum = sum + i
    assert(sum == 10)

    let xs = int32[3] { 10, 20, 30 }
    let mut total = 0
    for x in xs do total = total + x
    assert(total == 60)
    0
