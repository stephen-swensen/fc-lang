let main = (args: str[]) ->
    let _result =
        let data = i32[3] { 10, 20, 30 }
        let mut sum = 0i64
        for i, x in data do
            sum = sum + i + (i64) x
        (i32) sum
    assert(_result == 63)
    0
