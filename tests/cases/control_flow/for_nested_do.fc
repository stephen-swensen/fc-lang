// Nested `for` loops: `do` is required at every level and composes across the
// block form (outer) and the inline form (inner).
let main = (args: str[]) ->
    let mut sum = 0
    for i in 0..3 do
        for j in 0..3 do sum = sum + i * j
    assert(sum == 9)
    0
