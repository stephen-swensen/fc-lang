// Bare `ok` (no parens) constructs the payload-less void! result —
// the expression twin of the bare `ok` pattern.
let main = (args: str[]) ->
    let r = ok
    match r with
    | ok -> 0
    | err(_) -> 1
