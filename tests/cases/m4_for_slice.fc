let main = (args: int32) ->
    let mut data = int32[3] { }
    data.ptr[0] = 10
    data.ptr[1] = 20
    data.ptr[2] = 30
    let mut sum = 0
    for i, x in data
        if i == 1i64 then sum = sum + x
    sum
