struct item =
    name: int32
    count: int32?

let main = (args: str[]) ->
    let _result =
        let i = item { name = 42, count = default(int32?) }
        match i with
        | { count = some(c) } -> c
        | { name = n, count = none } -> n
    assert(_result == 42)
    0
