struct item =
    name: i32
    count: i32?

let main = (args: str[]) ->
    let _result =
        let i = item { name = 42, count = none(i32) }
        match i with
        | { count = some(c) } -> c
        | { name = n, count = none } -> n
    assert(_result == 42)
    0
