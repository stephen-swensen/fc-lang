struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let _result =
        let p = some(point { x = 3, y = 7 })
        match p with
        | some({ x = 0, y = y }) -> y
        | some({ x = x, y = y }) -> x + y
        | none -> 0
    assert(_result == 10)
    0
