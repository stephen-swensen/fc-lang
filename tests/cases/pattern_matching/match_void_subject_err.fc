// A match subject is a value position: codegen binds it to a temporary, so a
// void subject would emit `void _subj0 = f(...);`. Rejected in pass2, like
// void in a let binding or a call argument.
let f = () -> void()

let main = (args: str[]) ->
    match f() with
    | _ -> void()
    0
