// The other spelling that reaches a void subject: an assignment expression is
// void-typed, so `match (n = 5) with` is the same hole as a void call.
let main = (args: str[]) ->
    let mut n = 1
    match (n = 5) with
    | _ -> void()
    0
