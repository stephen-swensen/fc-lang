struct point =
    x: i32
    y: i32

union shape =
    | circle(i32)
    | rect(point)

let main = (args: str[]) ->
    let _result =
        let s = shape.rect(point { x = 4, y = 6 })
        match s with
        | circle(r) -> r
        | rect({ x = x, y = y }) -> x + y
    assert(_result == 10)
    0
