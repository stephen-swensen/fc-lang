// Multiline struct literal using continuation indentation
struct point =
    x: i32
    y: i32
    z: i32

let main = (args: str[]) ->
    let _result =
        let p = point {
                x = 10, y = 20,
                z = 30 }
        p.x + p.y + p.z
    assert(_result == 60)
    0
