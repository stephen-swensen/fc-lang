struct item =
    name: int32
    count: int32?

let main = (args: str[]) ->
    let _result =
        let i = item { name = 1, count = some(10) }
        let result = match i with
        | { count = some(c) } -> c
        | { count = none } -> 200
        result
    assert(_result == 10)
    0
