// Same generic function instantiated with different types
let get_bits = <'a>() -> 'a.bits

let main = (args: str[]) ->
    let a = get_bits<u8>()
    let b = get_bits<i32>()
    // 8 + 32 = 40
    assert(a + b == 40)
    0
