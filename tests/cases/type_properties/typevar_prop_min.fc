// Basic: 'a.min on integer type variable
let get_min = <'a>() -> 'a.min

let main = (args: str[]) ->
    let m = get_min<i32>()
    // i32.min = -2147483648, but exit codes are mod 256
    // just check it compiles and runs; use u8 for checkable exit
    let n = get_min<u8>()
    (n + 0)
