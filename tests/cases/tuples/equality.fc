let main = (args: str[]) ->
    assert({ 1, 2 } == { 1, 2 })
    assert({ 1, 2 } != { 1, 3 })
    // nested equality recurses
    assert({ { 1, 2 }, 3 } == { { 1, 2 }, 3 })
    assert({ { 1, 2 }, 3 } != { { 1, 9 }, 3 })
    0
