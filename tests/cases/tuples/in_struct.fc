struct box =
    pair: {i32, i32}
    tag: i32

let main = (args: str[]) ->
    let b = box { pair = { 3, 4 }, tag = 1 }
    assert(b.pair[0] == 3)
    assert(b.pair[1] == 4)
    assert(b.tag == 1)
    0
