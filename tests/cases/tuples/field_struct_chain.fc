// Top-level struct -> tuple{point, i32} -> top-level struct point.
// Requires the codegen topo-sort to emit point, then the tuple, then holder
// (the tuple is a monomorphized def, the structs are top-level defs).
struct point =
    x: i32
    y: i32
struct holder =
    pair: {point, i32}
    tag: i32
let main = (args: str[]) ->
    let h = holder { pair = { point { x = 3, y = 4 }, 9 }, tag = 1 }
    assert(h.pair[0].x == 3)
    assert(h.pair[0].y == 4)
    assert(h.pair[1] == 9)
    assert(h.tag == 1)
    0
