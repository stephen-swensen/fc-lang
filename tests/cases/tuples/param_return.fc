let first = (p: {i32, i32}) -> p[0]
let make = (x: i32) -> { x, x * 2 }

let main = (args: str[]) ->
    assert(first({ 7, 8 }) == 7)
    let m = make(5)
    assert(m[0] == 5)
    assert(m[1] == 10)
    0
