// shift amount widening: i32 literal widens into usize shift
let main = (args: str[]) ->
    let a = 4usize
    let b = a << 2       // RHS is i32 literal; widens for shift
    let c = a >> 1
    assert(b == 16usize)
    assert(c == 2usize)
    0
