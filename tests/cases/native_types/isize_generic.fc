let identity = (x: 'a) -> x

let add_them = (a: 'a, b: 'a) -> a + b

let main = (args: str[]) ->
    let a = identity(42isize)
    let b = identity(10usize)
    let c = add_them(20isize, 22isize)
    let d = add_them(5usize, 7usize)
    assert((i32) a + (i32) b + (i32) c + (i32) d == 106)
    0
