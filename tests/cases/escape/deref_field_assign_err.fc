// (*h).p is the same heap store as h->p — both must be rejected
struct holder =
    p: i32*

let leak = () ->
    let h = alloc(holder { p = default(i32*) })!
    let mut x = 42
    (*h).p = &x
    free(h)

let main = (args: str[]) ->
    leak()
    0
