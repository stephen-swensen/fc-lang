// Error: assigning a struct literal that contains a stack pointer to a global
struct holder =
    p: i32*

let mut gh = holder { p = default(i32*) }

let leak = () ->
    let mut x = 42
    gh = holder { p = &x }

let main = (args: str[]) ->
    leak()
    0
