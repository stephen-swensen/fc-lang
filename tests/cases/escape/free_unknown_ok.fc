// Freeing a pointer received from a function is fine (PROV_UNKNOWN)
let get_ptr = () ->
    let p = alloc(int32)!
    *p = 55
    p

let main = (args: str[]) ->
    let p = get_ptr()
    let val = *p
    free(p)
    val
