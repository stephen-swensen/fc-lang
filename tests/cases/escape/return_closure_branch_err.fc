// If-expression where one branch captures — conservative merge
let make = (x: i32, flag: bool) ->
    if flag then
        (n: i32) -> n + x
    else
        (n: i32) -> n * 2

let main = (args: str[]) ->
    0
