// Positive: assigning a heap pointer to a heap struct's field is fine
struct holder =
    p: i32*

let main = (args: str[]) ->
    let h = alloc(holder { p = default(i32*) })!
    let p = alloc(i32)!
    *p = 42
    h->p = p
    let v = *h->p
    free(h->p)
    free(h)
    assert(v == 42)
    0
