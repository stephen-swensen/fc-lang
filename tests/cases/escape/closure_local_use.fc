// Capturing closures used locally — no escape, no error
let main = (args: str[]) ->
    let threshold = 10
    let above = (x: i32) -> x > threshold
    assert(above(15) == true)
    assert(above(5) == false)
    0
