// A mut `const cstr` reassigned from a stack (cstr[N]) cast carries the stack
// taint through the widen; returning it must be rejected.
let bad = (p: const str) ->
    let mut c = c"xxxxxxxx"
    c = (cstr[8]) p
    c

let main = (args: str[]) ->
    let x = bad("hi")
    0
