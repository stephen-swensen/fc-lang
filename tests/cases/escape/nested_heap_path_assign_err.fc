// A nested lvalue path rooted at a heap pointer must be walked to its root
struct inner =
    p: i32*
struct outer =
    i: inner

let leak = () ->
    let ho = alloc(outer { i = inner { p = default(i32*) } })!
    let mut x = 42
    ho->i.p = &x
    free(ho)

let main = (args: str[]) ->
    leak()
    0
