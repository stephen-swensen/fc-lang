// Error: returning a stack struct literal whose field holds a stack slice
struct box =
    data: i32[]

let bad = () ->
    let arr = i32[3] { 1, 2, 3 }
    box { data = arr }

let main = (args: str[]) ->
    let _b = bad()
    0
