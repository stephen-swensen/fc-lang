// Reassigning a mut binding to a stack address must taint it for the global sink
let mut gp = default(i32*)

let bad = (param: i32*) ->
    let mut p = param
    let mut x = 5
    p = &x
    gp = p

let main = (args: str[]) ->
    bad(default(i32*))
    0
