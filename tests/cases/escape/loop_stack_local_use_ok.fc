// A mut binding holding a stack address is fine when only read locally (no sink)
let main = (args: str[]) ->
    let mut p = default(i32*)
    let mut sum = 0
    for i in 0..4 do
        let mut x = i
        p = &x
        sum = sum + *p
    assert(sum == 6)
    0
