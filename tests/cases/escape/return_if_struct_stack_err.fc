// Error: if-branch stack struct propagates through merge_prov
struct holder =
    p: i32*

let bad = (flag: bool) ->
    let mut x = 42
    if flag then
        holder { p = &x }
    else
        holder { p = default(i32*) }

let main = (args: str[]) ->
    let _h = bad(true)
    0
