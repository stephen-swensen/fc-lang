// Heap pointer in heap struct is fine
struct node =
    data: int32*

let main = (args: str[]) ->
    let p = alloc(int32)!
    *p = 42
    let n = alloc(node { data = p })!
    let result = *(n->data)
    free(p)
    free(n)
    assert(result == 42)
    0
