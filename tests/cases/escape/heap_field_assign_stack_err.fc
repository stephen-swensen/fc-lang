// Error: assigning a stack pointer to a heap-struct's field via ->
struct holder =
    p: i32*

let leak = () ->
    let h = alloc(holder { p = default(i32*) })!
    let mut x = 42
    h->p = &x
    free(h)

let main = (args: str[]) ->
    leak()
    0
