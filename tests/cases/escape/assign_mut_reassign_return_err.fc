// Reassigning a mut binding to a stack address must taint it for the return sink
let bad = (param: int32*) ->
    let mut p = param
    let mut x = 5
    p = &x
    return p

let main = (args: str[]) ->
    let mut y = 7
    let r = bad(&y)
    0
