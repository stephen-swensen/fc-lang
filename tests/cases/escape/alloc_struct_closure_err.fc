// Storing a capturing closure in a heap-allocated struct
struct callback =
    f: (i32) -> i32

let main = (args: str[]) ->
    let x = 10
    let cb = alloc(callback { f = (n: i32) -> n + x })
    free(cb!)
    0
