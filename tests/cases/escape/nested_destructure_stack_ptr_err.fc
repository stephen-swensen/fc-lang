// Provenance reaches bindings at any destructure depth, not just the top level.
let bad = () ->
    let mut x = 41
    x = x + 1
    let nested = { { &x, 1 }, 2 }
    let { { p, a }, b } = nested
    let _s = a + b
    p

let main = (args: str[]) ->
    let _r = bad()
    0
