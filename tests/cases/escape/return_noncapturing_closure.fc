// Non-capturing closures are safe to return — no context to dangle
let make_doubler = () ->
    (x: int32) -> x * 2

let main = (args: str[]) ->
    let f = make_doubler()
    assert(f(21) == 42)
    0
