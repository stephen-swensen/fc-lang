// alloc(interp_string) promotes stack to heap — safe to return
let make_str = (n: i32) ->
    let s = "%d{n}"
    alloc(s)!

let main = (args: str[]) ->
    let s = make_str(7)
    let result = (i32) s.len
    free(s)
    assert(result == 1)
    0
