// Returning a heap pointer is fine
let make_ptr = (x: i32) ->
    let p = alloc(i32)!
    *p = x
    p

let main = (args: str[]) ->
    let p = make_ptr(42)
    let val = *p
    free(p)
    assert(val == 42)
    0
