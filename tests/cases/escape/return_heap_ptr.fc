// Returning a heap pointer is fine
let make_ptr = (x: int32) ->
    let p = alloc(int32)!
    *p = x
    p

let main = (args: str[]) ->
    let p = make_ptr(42)
    let val = *p
    free(p)
    assert(val == 42)
    0
