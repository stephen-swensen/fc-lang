// Reassigning a mut binding to a heap pointer keeps it returnable
let to_heap = (param: i32*) ->
    let mut p = param
    let q = alloc(i32)!
    *q = 99
    p = q
    return p

let main = (args: str[]) ->
    let r = to_heap(default(i32*))
    let v = *r
    free(r)
    assert(v == 99)
    0
