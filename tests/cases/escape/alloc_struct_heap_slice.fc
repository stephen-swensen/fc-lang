// heap slice stored in heap struct is fine
struct holder =
    data: int32[]

let main = (args: str[]) ->
    let buf = alloc(int32[3] { 1, 2, 4 })!
    let h = alloc(holder { data = buf })!
    let sum = h->data[0] + h->data[1] + h->data[2]
    free(buf)
    free(h)
    assert(sum == 7)
    0
