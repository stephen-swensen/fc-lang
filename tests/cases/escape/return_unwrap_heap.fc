// Unwrapping a heap option preserves heap provenance
let make = () ->
    let p = alloc(int32)!
    *p = 99
    p

let main = (args: str[]) ->
    let p = make()
    let val = *p
    free(p)
    assert(val == 99)
    0
