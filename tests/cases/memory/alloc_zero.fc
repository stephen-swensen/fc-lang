struct counters =
    a: i32
    b: i32
    c: i32

let main = (args: str[]) ->
    let _result =
        let p = alloc(counters)!
        let sum = p->a + p->b + p->c
        free(p)
        if sum == 0 then 1 else 0
    assert(_result == 1)
    0
