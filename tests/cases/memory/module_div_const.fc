// Integer division in a module-level const-expr is evaluated at compile time
// to a literal, so it is a valid C file-scope initializer.
module m =
    let x = 10 / 2

let main = (args: str[]) ->
    assert(m.x == 5)
    0
