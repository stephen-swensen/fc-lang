// File-level array literal — backed by alloca in C main
let arr = int32[3] { 10, 20, 30 }

let main = (args: str[]) ->
    assert(arr[0] == 10)
    assert(arr[1] == 20)
    assert(arr[2] == 30)
    assert(arr.len == 3)
    0
