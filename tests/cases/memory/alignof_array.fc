let main = (args: str[]) ->
    // alignof(T[N]) returns alignment of element type T, not the array
    assert(alignof(i32[4]) == alignof(i32))
    assert(alignof(i64[8]) == alignof(i64))
    assert(alignof(i8[16]) == alignof(i8))

    0
