let main = (args: str[]) ->
    let af32 = alignof(f32)
    let af64 = alignof(f64)

    // f32 alignment is at least 4
    assert(af32 >= 4i64)

    // f64 alignment is at least 8
    assert(af64 >= 8i64)

    0
