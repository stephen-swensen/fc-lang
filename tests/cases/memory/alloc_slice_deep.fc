let main = (args: str[]) ->
    let mut buf = int32[3] { }
    buf.ptr[0] = 10
    buf.ptr[1] = 20
    buf.ptr[2] = 30

    let copy = alloc(buf)!

    buf.ptr[0] = 99

    let v = copy[0] + copy[1] + copy[2]
    free(copy)
    assert(v == 60)
    0
