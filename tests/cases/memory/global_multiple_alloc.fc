// Multiple file-level globals with alloc — tests init ordering
let a = alloc(i32)!
let b = alloc(i32)!

let main = (args: str[]) ->
    *a = 10
    *b = 20
    assert(*a == 10)
    assert(*b == 20)
    assert(*a + *b == 30)
    free(a)
    free(b)
    0
