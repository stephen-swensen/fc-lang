struct point =
    x: i32
    y: i32

let add = (a: i32, b: i32) -> a + b

let main = (args: str[]) ->
    let a = 1 + true
    let b = a + 2
    let c = add(a, 3)
    let p = point { x = a, y = b }
    0
