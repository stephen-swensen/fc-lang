/* Top-level block comment */
let add = (a: i32, b: i32) ->
    /* nested /* inner block comment */ end */
    a + b /* inline comment */

let main = (args: str[]) ->
    let _result =
        /* multi-line
           block comment */
        add(0, 1)
    assert(_result == 1)
    0
