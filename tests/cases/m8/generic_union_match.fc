union maybe =
    | just('a)
    | nothing

let from_maybe = (m: maybe<'a>, fallback: 'a) ->
    match m with
    | just(v) -> v
    | nothing -> fallback

let main = (args: int32) ->
    let x = maybe<int32>.just(42)
    let y = maybe<int32>.just(10)
    from_maybe(x, 0) + from_maybe(y, 0)
