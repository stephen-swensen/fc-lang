let apply = (f: (i32) -> i32, x: i32) -> f(x)

let main = (args: str[]) ->
    let offset = 5
    let add_offset = (n: i32) -> n + offset
    let a = apply(add_offset, 10)
    let b = apply(add_offset, 20)
    assert(a + b + 2 == 42)
    0
