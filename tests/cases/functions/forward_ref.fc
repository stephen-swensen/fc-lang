// Forward reference: a calls b, b is defined later.
// Both have inferred return types.
let a = (x: int32) -> b(x) + 1
let b = (x: int32) -> x * 2

let main = (args: str[]) ->
    // a(5) = b(5) + 1 = 10 + 1 = 11
    assert(a(5) == 11)
    // b(3) = 6
    assert(b(3) == 6)
    0
