// §7.8: a module named after a built-in type name is rejected — `i32.member`
// resolves to type-property lookup, so the module would be unreachable.
module i32 =
    let helper = () -> 3

let main = (args: str[]) ->
    0
