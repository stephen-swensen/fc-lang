// §7.8: an error group desugars to a module, so one named after a built-in
// type name is rejected for the same reason a module is.
error str =
    | boom
    | splat

let main = (args: str[]) ->
    0
