// §7.8: an extern struct's FC-visible name may not be a built-in type name —
// the same unreachability applies to a C-interop type.
module m from "stddef.h" =
    extern struct i32 =
        x: i32

let main = (args: str[]) ->
    0
