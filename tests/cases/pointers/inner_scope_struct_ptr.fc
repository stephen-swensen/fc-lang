struct point =
    x: i32
    y: i32

let main = (args: str[]) ->
    let p =
        let mut pt = point { x = 3, y = 4 }
        &pt
    assert(p->x == 3)
    assert(p->y == 4)
    p->x = 30
    assert(p->x == 30)
    0
