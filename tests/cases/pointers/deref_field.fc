struct point =
    x: int32
    y: int32

let set_point = (p: point*, nx: int32, ny: int32) ->
    p->x = nx
    p->y = ny

let main = (args: str[]) ->
    let mut p = point { x = 0, y = 0 }
    set_point(&p, 3, 7)
    assert(p.x + p.y == 10)
    0
