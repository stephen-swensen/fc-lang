struct point =
    x: i32
    y: i32

let set_point = (p: point*, nx: i32, ny: i32) ->
    p->x = nx
    p->y = ny

let main = (args: str[]) ->
    let mut p = point { x = 0, y = 0 }
    set_point(&p, 3, 7)
    assert(p.x + p.y == 10)
    0
