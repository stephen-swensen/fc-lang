let main = (args: str[]) ->
    let x = 2
    let p = match x with
    | 1 ->
        let mut a = 10
        &a
    | 2 ->
        let mut b = 20
        &b
    | _ ->
        let mut c = 30
        &c
    assert(*p == 20)
    0
