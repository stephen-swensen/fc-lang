// atomic_load_acquire is permitted through a const pointer — loads are reads.

let peek = (p: const usize*) -> atomic_load_acquire(p)

let main = (args: str[]) ->
    let mut tail = 0usize
    atomic_store_release(&tail, 17usize)
    assert(peek(&tail) == 17usize)
    0
