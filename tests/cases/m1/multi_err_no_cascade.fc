struct point =
    x: int32
    y: int32

let add = (a: int32, b: int32) -> a + b

let main = (args: int32) ->
    let a = 1 + true
    let b = a + 2
    let c = add(a, 3)
    let p = point { x = a, y = b }
    0
