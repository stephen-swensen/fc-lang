let check = (a: int32, b: int32) ->
    if a > 0 && b > 0 then
        1
    else if a > 0 || b > 0 then
        2
    else
        3

let main = (args: int32) ->
    let r1 = check(5, 3)
    let r2 = check(5, -1)
    let r3 = check(-1, -2)
    r1 + r2 * 10 + r3
