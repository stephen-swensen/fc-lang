let check = (x: int32??) ->
    match x with
    | some(some(v)) -> v
    | some(none) -> 0
    | none -> -1

let main = (args: str[]) ->
    let a = some(some(42))
    let b = some(default(int32?))
    let c = default(int32??)
    check(a) + check(b) + check(c)
