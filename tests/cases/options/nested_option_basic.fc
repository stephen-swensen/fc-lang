let check = (x: i32??) ->
    match x with
    | some(some(v)) -> v
    | some(none) -> 0
    | none -> -1

let main = (args: str[]) ->
    let a = some(some(42))
    let b = some(none(i32))
    let c = none(i32?)
    assert(check(a) + check(b) + check(c) == 41)
    0
