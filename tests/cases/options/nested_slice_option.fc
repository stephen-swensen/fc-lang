let check = (x: i32[]?) ->
    match x with
    | some(s) -> s[0]
    | none -> -1

let main = (args: str[]) ->
    let arr = i32[3] { 50, 60, 70 }
    let a = some(arr)
    let b = none(i32[])
    assert(check(a) + check(b) == 49)
    0
