let main = (args: str[]) ->
    let mut data = int32[3] { }
    data.ptr[0] = 10
    data.ptr[1] = 20
    data.ptr[2] = 30
    let mut sum = 0
    for i, x in data
        if i == 1 then sum = sum + x
    sum
