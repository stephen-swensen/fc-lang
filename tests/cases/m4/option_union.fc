union color =
    | red
    | green
    | blue

let check = (c: color?) ->
    match c with
    | some(v) ->
        match v with
        | red -> 1
        | green -> 42
        | blue -> 3
    | none -> 0

let main = (args: str[]) ->
    let c = some(color.green)
    check(c)
