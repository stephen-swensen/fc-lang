import * from std::prelude

#if main1
let main_version = "main1"
let main = (args: str[]) ->
    println(main_version)
    assert(main_version == "main1")
    0
#else
let main_version = "main_else"
let main = (args: str[]) ->
    println(main_version)
    assert(main_version == "main_else")
    0
#end

